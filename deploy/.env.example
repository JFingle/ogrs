# Copy to deploy/.env and fill in.
# .env is gitignored — never commit real credentials.

MARIADB_DATABASE=ogrs
MARIADB_ROOT_USER=root
MARIADB_ROOT_PASSWORD=changeme-not-root
MARIADB_USER=ogrs
MARIADB_PASSWORD=changeme-too
